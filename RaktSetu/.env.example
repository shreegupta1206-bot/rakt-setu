# Frontend environment — copy to .env
# With vite.config.ts proxy you DON'T need this in dev (proxy handles /api).
# Only needed if you deploy frontend separately from the server.
VITE_API_URL=http://localhost:8000
